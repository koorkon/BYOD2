# Local .terraform directories (plugins and modules)
**/.terraform/*

# .tfstate files (Contains sensitive current state - NEVER commit these)
*.tfstate
*.tfstate.*

# Crash logs
crash.log
crash.log.*

# Exclude all .tfvars files, which often contain secrets/passwords.
# If you have a 'terraform.tfvars' with only non-sensitive data, 
# you can remove this line, but be careful.
*.tfvars
*.tfvars.json

# Ignore plan files (generated by 'terraform plan -out=...')
*.tfplan

# Ignore override files as they are usually local overrides
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration files
.terraformrc
terraform.rc

# VS Code settings (optional, but good practice)
.vscode/